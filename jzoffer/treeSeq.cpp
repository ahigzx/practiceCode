class Solution {
    public:
        char* Serialize(TreeNode *root) {
            if(root == NULL)
                return strcpy(str, "$");
        }
        TreeNode* Deserialize(char *str) {
        }
};
